注文履歴一覧
<table class="table">
    <thead>
        <tr>
            <th>購入日時</th>
            <th>購入者</th>
            <th>注文個数</th>
            <th>注文ステータス</th>
        </tr>
    </thead>
    
    <tbody>
        <% @orders.each do |order| %>
            <tr>
                <td>
                 <%= link_to admin_order_path(order_id) do %>
                    <%= order.created_at %>
                 <% end %>
                </td>
                <td><%= order.customer.first_name %></td>
                <td><%= order.amount %></td>
                <td><%= order.production_status %></td>
            </tr>
        <% end %>
    </tbody>
</table>

<%= paginate @orders %>